# Class attributes can be assigned only if they have the same type

begin
  class H is 
    public int v
    H(int v) 
    end
    private int J(int v) is 
      return 0
    end
  end
  H p = new H(2);
  bool x = p.v
end